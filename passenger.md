inherits from `person`

```json
{
    "is_driver": {
            "type": "boolean",
            "description": "Whether the person drove the car at some point."
        },
    "reason_to_pick_up_or_reject": {
            "type": "string",
            "description": "The reasone the hitchhiker(s) are picked up or rejected identified by asking them specifically."
        }
}

```